# Fungsi ini digunakan untuk menampilkan semua network yang ada
docker network ls

# Fungsi ini digunakan untuk membuat network baru
docker network create --driver namaDriver namaNetwork

# Fungsi ini digunakan untuk menghapus network yang telah dibuat
docker network rm namaNetwork